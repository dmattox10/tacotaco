require('dotenv').config()

const {
  APP_NAME,
  APP_PORT,
  DB_HOST,
  DB_USER,
  DB_PASS,
  DB_NAME,
  EMAIL_HOST,
  EMAIL_USER,
  EMAIL_PASS,
  ENVIRONMENT,
  REFRESH_SECRET,
  SHARED_SECRET,
  USE_AUTH,
  WHITELIST_URLS
} = process.env

module.exports = {
  APP_PORT,
  APP_NAME,
  DB_HOST,
  DB_USER,
  DB_PASS,
  DB_NAME,
  EMAIL_HOST,
  EMAIL_USER,
  EMAIL_PASS,
  ENVIRONMENT,
  REFRESH_SECRET,
  SHARED_SECRET,
  USE_AUTH,
  WHITELIST_URLS
}